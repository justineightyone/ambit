# Progress
Status: Current
Last reviewed: 2026-09-01

## Current Baseline
- The current release manifests are version `0.10.0`: `package.json`, both Tauri configuration files, `src-tauri/Cargo.toml`, the local `app` package in `src-tauri/Cargo.lock`, and `.github/.release-please-manifest.json` agree. Hosted release state still belongs to GitHub rather than this file.
- Release builds remain Windows-only. Linux and macOS packages are manual, unsigned or non-updater experimental artifacts as documented in `docs/experimental-unix-builds.md`.
- Production packaging runs `verify:release` before Tauri builds. The gate checks version consistency, generated binding drift, lint, TypeScript, guarded frontend output, coverage, Rust tests, and a no-bundle Tauri compatibility build.
- ComfyUI metadata milestones 22 through 61 shipped in `v0.10.0`. The pinned v0.11.18 core target remains all 93 workflows: 86 golden, 2 pattern-covered, 5 partial, and 0 unassessed. The separate 16-workflow extended image scope contains 15 golden and 1 partial, bringing the whole manifest to 101 golden, 2 pattern-covered, 6 partial, and 469 excluded. The SUPIR partial retains exact sampler metadata but cannot recover its selected generated caption because the `TextGenerate` result is not embedded. The two remaining pattern-covered entries are intentionally disconnected or bypassed starter workflows. Non-generative utility outputs suppress weak sampler/global fallback when the authored workflow contains no sampler definition, preventing utility checkpoints and selector text from becoming generation metadata. The path-parity gate covers 109 official catalog fixtures and 21 real-world fixtures across direct extraction, scanner-style merging, reparse, and developer diagnostics. Parser version remains 46. Milestones 62 through 66 provide the local support-bundle export, offline replay and differences, and guarded fixture-candidate preparation and inspection workflow. Milestone 67 documents its privacy boundary and protects default support-bundle filenames from accidental Git inclusion. Milestones 68 through 74 make the Image Viewer consume the same Rust-normalized ComfyUI graph for API prompts and workflow-only subgraphs, group namespaced internal nodes, navigate deterministic connections, identify parser-selected outputs and root samplers, focus their connected dependency branch, and navigate core-field, resource, and traversal-blocker evidence while preserving the exact archival workflow for copy and download. Milestones 75 through 79 keep older schema-1 support bundles verifiable when they predate optional provenance diagnostics, require every diagnostics field to declare a replay policy, distinguish extracted-metadata drift from observability-only drift, provide value-redacted replay summaries, replay bounded private directories through deterministic hash-only reports, and group repeated drift shapes for cohort-based triage. See `docs/comfyui-support-workflow.md` and `docs/plans/milestone-68.md` through `docs/plans/milestone-79.md`. These observability and display milestones do not change parser output or trigger metadata refresh.
- The search-transition, prompt-masking, setup-guide replay, and tooltip-dismissal packages recorded in `docs/plans/release-0.9.0-ux-readiness.md` landed before the `v0.9.0` release.
- Removal and deletion now use coordinated native lifecycle commands. Removing or restoring preserves a retryable tombstone boundary, file hash, parser version, memberships, and resource links; final deletion retains records when OS trash fails; already-missing files can be safely reconciled on retry; collection membership moves are atomic; and facet rebuilds swap from staging under a shared full/incremental coordinator.
- Thumbnail repair now uses migration-79 canonical candidate views over disjoint partial indexes, incremental keyset discovery, and set-based atomic persistence. Smart starts with truthful indeterminate discovery, reports typed processing/persisting/throttled phases and fetch/encode/database timing, and no longer performs a full candidate count at startup. Maintenance regeneration submits scoped keyset-paged IDs to the same native engine at Fast priority; selected regeneration force-overwrites cached files and all manual regeneration retries failures immediately. One native job lease and cancellation command coordinate both orchestrators. Confirmed missing files preserve cached thumbnails and rows, temporarily unavailable monitored or InvokeAI roots are deferred, and each committed operation refreshes image, statistic, thumbnail-facet, and collection-thumbnail consumers once. The expanded production-catalog gate measured cold Smart discovery of 500 candidates at about 1.48 seconds, Maintenance validation of those 500 IDs at about 18 ms, and persistence of 100 mixed outcomes at about 35 ms on the disposable 11.4 GB catalog copy.
- InvokeAI synchronization now applies one durable legacy, All users, or selected-owner scope across manual, startup, and Live Watch queries. Selected-owner mode includes owned boards and collections, disables filesystem-only orphan recovery without erasing its preference, and preserves hidden rows for later scope changes. InvokeAI board collections now reconcile authoritative source names and memberships through durable local name/membership overlays: source-unavailable boards retain their last effective projection, local edits survive every sync mode, deletion becomes recoverable owner-scoped suppression, Reset restores current source state, and tombstoned or physically missing source images are not re-imported.
- InvokeAI image source classification, asset visibility, high-value metadata extraction, reference provenance/navigation, and owner-scope startup remediation are implementation-complete and release-verified. Brief database, owner, and privacy checks remain behind the branded splash; sustained preparation is delayed, progress is stable, and the handoff into background catch-up is explicit. The real-data desktop restart is owner-accepted; multi-owner selection and temporary-offline retry remain recovery acceptance. See `docs/plans/invokeai-image-assets-and-metadata.md` and `docs/plans/invokeai-owner-scope-startup-remediation.md`.

## Current Constraints
- Specta binding generation is explicit. Do not expect a debug Tauri launch to update `src/bindings.ts`; run `pnpm run bindings:generate`, then `pnpm run bindings:check`.
- Desktop persistence is intentionally split: SQLite stores image records and heavy metadata under Local AppData, `library.json` stores lightweight app settings and recent searches, and the OS keyring stores sensitive API keys.
- `src/services/repository.ts` is not the shipping desktop persistence path. Treat its LocalStorage/mock behavior as an ambiguous fallback until a dedicated task either validates or retires it.
- Exact duplicate detection is a global SHA-256 scan. Cleanup merges safe keeper state and collection memberships, moves redundant records through the Removed flow, and does not delete files by default.
- `docs/plans/viewer-settings-ux-followup.md` records the completed post-palette interaction closure: rounded Gallery privacy clipping, a single formatted prompt read/edit surface, responsive Workflow anchors with collapsed in-flow developer diagnostics, flicker-free Settings navigation with reduced-motion support, and stable Settings category selection borders and consistent focus presentation for the Gallery workspace. Automated verification, owner desktop smoke, and publication approval are complete on `codex/viewer-settings-ux-followup`.
- The `io.github.asuraace.ambit` identifier is current. Startup migration and reset/repair paths still account for legacy `com.ambit.app` Local and Roaming AppData during the public-beta transition.

## Active Follow-Ups
- `docs/plans/genai-video-library-support.md` is the active GenAI video-support plan. WP1 manual import-to-viewer support and WP2 folder discovery, typed Live Watch convergence, mixed-library operations, and aligned image/video privacy behavior are accepted. Owner smoke and closure review covered create, modify, rename, removal, filters, collections, flags, Removed/restore, export, privacy gating, Windows normal/verbatim/UNC identity, and Maintenance video routing. The follow-up assurance pass made poster extraction and large-file exact hashing promptly cancellable, avoids redundant duplicate poster decoding, and prevents posterless source-video URLs from reaching image-only thumbnail elements. The momentary `Shift+H` privacy-protection frame remains intentionally fail-closed. Restore preserves original ordering through query-driven refresh, rechecks source availability, and restores absent local media as missing. WP3 ComfyUI-first metadata/provenance is implementation-complete with six pinned mode fixtures, strict exact-sidecar validation, preserved evidence and conflicts, independent video parser versioning, resource-facet persistence, Live Watch sidecar refresh, explicit overrides/revert, and video Details/Metadata/Workflow surfaces. Owner smoke found and remediated reveal/playback reset on metadata edits, lightweight generation-mode restart/revert drift, image-only diagnostics receiving video sidecar evidence, and empty lightweight resource arrays masking full provenance. Metadata hierarchy and collection/resource presentation were refined for owner evaluation, with a development-only populated-resource preview that does not modify stored metadata. Image and video viewers now share the searchable, privacy-aware collection membership picker, weighted resource-chip presentation, prompt/notes field presentation, compact workflow inspector, text-only keyboard-accessible tabs, sidebar structure, toolbar framing, shortcut ownership, and generator/model override editors while retaining their distinct canvas/playback behavior and media-specific actions. Model choices come from the global checkpoint facet inventory with a custom option; blank, cancelled, and unchanged edits do not persist or create overrides. Generation Parameters, Smart Tags, and populated resources are shared expanded-by-default disclosures with visible counts and viewer-session state retention. Both use Details, Metadata, and Workflow; the image Metadata tab follows the preferred compact video hierarchy for generator, model, prompts, generation parameters, provenance, and resources, while image technical facts, notes, palette, and collections live under Details. The same editing and resource-filtering capabilities are wired when either media type is opened from Maintenance. Empty resources are omitted; populated LoRA, ControlNet, and IP-Adapter chips filter the gallery; provenance uses accessible source badges aligned consistently at the far right; metadata revert appears only for real user overrides; original-prompt inspection is read-only; and unchanged blur events no longer create overrides or redundant saves. Both media viewers now open on Metadata, retain explicitly selected tabs and metadata disclosures during next/previous navigation, place prompts first, and use a source-neutral missing-workflow message. The shared sidebar gives metadata an independent scrolling body while keeping the image Creative Assistant footer visible, and Copy Parameters remains available in the Generation Parameters disclosure header. The latest viewer, hook, Maintenance, App, and AppLayout checks pass 365/365; lint, strict TypeScript, browser interaction, and console checks are green. The full local suite reaches 3,163 passing tests with one intentional skip but retains 13 unrelated locale- or timing-sensitive failures across nine existing test files; none exercise viewer code. The prior complete release gate, including coverage, Rust tests, guarded production output, and the release-profile Tauri build, remains the last green release-level result. Focused owner desktop smoke remains before WP3 closure. WP4 has not started.
- Complete the remaining owner-acceptance recovery journey for InvokeAI owner scope: measure click-to-ready desktop timing for a restored and selectively repaired multi-owner switch after restart, then validate temporary offline startup, retry, and scope changes without missing or deleted Ambit records. Single-owner presentation, complete board reconciliation, logical-view switching, and persistent per-scope derived caches are implementation-complete. Changed shared InvokeAI database snapshots remain fail-closed and run the selected scope's catch-up because aggregate owner fingerprints cannot prove freshness. Prepared-snapshot restoration and the owner-scope migrations are verified against transactionally consistent copies of the real 1.4 GB dev catalog. The normal-restart and real-data cold-cache paths are accepted.
- Verification update (2026-08-12): this supersedes the earlier full-suite count in the milestone summary. The complete frontend suite now passes 3,192/3,192 tests with one intentional skip; the previously recorded locale/timing failures did not reproduce.
- `docs/plans/release-0.9.0-ux-readiness.md` was overtaken by the `v0.9.0` release and is no longer a live release gate. Its Work Package 3 (initial Smart Collection thumbnail hydration) and Work Package 4 (discoverable duplicate-group navigation) remain unversioned product follow-ups.
- Add browser smoke coverage for lazy-loaded app surfaces, including settings, statistics, maintenance, command palette, export, viewer, compare, recovery, slideshow, and collection editing.
- Add coverage thresholds after the public-beta baseline is intentionally reviewed.
- Add a small Tauri desktop launch smoke test using a temporary app-data/profile directory; keep installer and updater validation in the release-candidate workflow.
- Decide whether `src/services/repository.ts` remains a supported non-desktop/mock fallback or should be retired in dedicated cleanup.
- Keep structural follow-ups in `docs/refactor.md`; notably Live Watch pending-completion UX and facet-semantics centralization remain deferred there.

## Status Routing
- Use this file for moving repository state and near-term follow-ups.
- Use `docs/release-candidate-validation.md` for release-asset, updater, and installed-app evidence.
- Treat plans marked `Complete` or `Superseded` as historical. Do not infer active work from a pending item inside a superseded plan without reconciling it here.
- Use `docs/refactor.md` for actionable deferred structural work, not release status or session notes.
